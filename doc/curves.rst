Эллиптические кривые
=============================================

Библиотека ``libakrypt`` предоставляет инструменты для работы с эллиптическими кривыми, а так же функции для работы с точками и вычисления кратной точки, принадлежащей эллиптической кривой.

Используемые структуры
---------------------------------------

Структура wpoint, реализующая точку эллиптической кривой
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Структура предсталяет собой точку P эллиптической кривой, заданной в короткой форме Вейерштрасса,
в проективных координатах, т.е точка представляется в виде вектора :math:`P = (x:y:z)`,
удовлетворяющего сравнению :math:`y^{2}z \equiv x^3 + axz^2 + bz^3 \pmod{p}`.
В дальнейшем, при проведении вычислений, для координат точки используется
представление Монтгомери.

Структура wpoint определяется следующим образом

.. code-block:: c

 struct wpoint
 {
   ak_uint64 x[ak_mpzn512_size];
   ak_uint64 y[ak_mpzn512_size];
   ak_uint64 z[ak_mpzn512_size];
 };

Контекст точки эллиптической кривой в короткой форме Вейерштрасса обозначется как ak_wpoint
  
.. code-block:: c

 typedef struct wpoint *ak_wpoint

 
Поля структуры:

========= ========= =========
Поле      Тип       Описание
========= ========= =========
x         ak_uint64 x-координата точки эллиптической кривой
y         ak_uint64 y-координата точки эллиптической кривой
z         ak_uint64 z-координата точки эллиптической кривой
========= ========= =========

Структура wcurve, реализующая кривую, заданную в короткой форме Вейерштрасса
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 
Структура определяет эллиптическую кривую (в короткой форме Вейерштрасса), заданную сравнением
:math:`y^2 \equiv x^3 + ax + b \pmod{p}`, а также образующую точку, :math:`P = (X, Y, Z)` на этой кривой 
с заданным порядком :math:`q`. Порядок :math:`m` всей группы точек эллиптической кривой может 
быть определен из равенства :math:`m = dq`, где величину :math:`d` называют кофактором.

Параметры :math:`n, n_q, r_2` вводятся для оптимизации вычислений. Определим :math:`r = 2^{256}`
или :math:`r = 2^{512}`, тогда :math:`n \equiv n_0 \pmod{2^{64}}`, 
где :math:`n_0 \equiv -p^{-1} \pmod{r}`.

Величина :math:`r_2` удовлетворяет сравнению :math:`r_2 \equiv r^2 \pmod{p}`. 

Структура wcurve определяется следующим образом

.. code-block:: c

 struct wcurve
 {
   ak_uint32 size;
   ak_uint32 cofactor;
   ak_uint64 a[ak_mpzn512_size];
   ak_uint64 b[ak_mpzn512_size];
   ak_uint64 p[ak_mpzn512_size];
   ak_uint64 r2[ak_mpzn512_size];
   ak_uint64 q[ak_mpzn512_size];
   ak_uint64 r2q[ak_mpzn512_size];
   struct wpoint point;
   ak_uint64 n;
   ak_uint64 nq;
   const char *pchar;
 };

Контекст эллиптической кривой, заданной в короткой форме Вейерштрасса, обознается как ak_wcurve

.. code-block:: c

 typedef struct wcurve *ak_wcurve

Поля структуры:

===============  ========== =====================
Поле             Тип        Описание
===============  ========== =====================
size             ak_uint32  Размер параметров эллиптической кривой, исчисляемый количеством 64-х битных блоков.       
cofactor         ak_uint32  Кофактор эллиптической кривой - делитель порядка группы точек.
a                ak_uint64  Коэффициент a эллиптической кривой (в представлении Монтгомери)
b                ak_uint64  Коэффициент b эллиптической кривой (в представлении Монтгомери).
p                ak_uint64  Модуль p эллиптической кривой.
r2               ak_uint64  Величина r^2, взятая по модулю p и используемая в арифметике Монтгомери.
q                ak_uint64  Порядок q подгруппы, порождаемой образующей точкой P.
r2q              ak_uint64  Величина r^2, взятая по модулю q и используемая в арифметике Монтгомери.
point            wpoint     Точка P эллиптической кривой, порождающая подгруппу порядка q.
n                ak_uint64  Константа n, используемая в арифметике Монтгомери по модулю p.  
nq               ak_uint64  Константа n_q, используемая в арифметике Монтгомери по модулю q.
pchar            const char Строка, содержащая символьную запись модуля p (Используется для проверки корректного хранения параметров кривой в памяти).
===============  ========== =====================


Есть ли какие-то встроенные эллиптические кривые в libakrypt?
--------------------------------------------------------------
Да, в ``libakrypt`` есть несколько уже заданных эллиптических кривых 256 и 512 бит.

Эллиптический кривые 256 бит                         
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

- **id_tc26_gost_3410_2012_256_paramSetTest** - Параметры 256-ти битной эллиптической кривой из тестового примера ГОСТ Р 34.10-2012
- **id_tc26_gost_3410_2012_256_paramSetA** - Параметры 256-ти битной эллиптической кривой из рекомендаций Р 50.1.114-2016 (paramSetA).
- **id_rfc4357_gost_3410_2001_paramSetA** - Параметры 256-ти битной эллиптической кривой, определяемые RFC-4357, set A (вариант КриптоПро). 
- **id_rfc4357_gost_3410_2001_paramSetB** - Параметры 256-ти битной эллиптической кривой, определяемые RFC-4357, set B (вариант КриптоПро). 
- **id_rfc4357_gost_3410_2001_paramSetC** - Параметры 256-ти битной эллиптической кривой, определяемые RFC-4357, set C (вариант КриптоПро).
- **id_axel_gost_3410_2012_256_paramSet_N0** - Параметры 256-ти битной эллиптической кривой, построенные в диссертационной работе Нестеренко А.Ю.

Эллиптические кривые 512 бит                         
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

- **id_tc26_gost_3410_2012_512_paramSetTest** - Параметры 512-ти битной эллиптической кривой из тестового примера ГОСТ Р 34.10-2012  
- **id_tc26_gost_3410_2012_512_paramSetA** - Параметры 512-ти битной эллиптической кривой из рекомендаций Р 50.1.114-2016 (paramSetA).
- **id_tc26_gost_3410_2012_512_paramSetB** - Параметры 512-ти битной эллиптической кривой из рекомендаций Р 50.1.114-2016 (paramSetB).
- **id_tc26_gost_3410_2012_512_paramSetC** - Параметры 512-ти битной эллиптической кривой из рекомендаций Р 50.1.114-2016 (paramSetC). 

Как задать кривую?
-----------------------------

Существует несколько способа задать эллиптическую кривую.

 - Использование существующих эллиптических кривых в ``libakrypt``
 - Определение параметров самостоятельно

Использование существующих кривых
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. code-block:: c

  struct wcurve mycurve;
  memcpy(
        &mycurve, 
        &id_tc26_gost_3410_2012_512_paramSetC, 
        sizeof(&id_tc26_gost_3410_2012_512_paramSetC)
  );

Вместо кривой id_tc26_gost_3410_2012_512_paramSetC может быть использована любая кривая, описанная выше


Определение параметров самостоятельно
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. code-block:: c

   struct wcurve mycurve = {
    ak_mpzn256_size,
    4, /* cofactor */
    { 0x6d0078e62fc81048LL, 0x94db4f98bfb73698LL, 0x75e9b60631449efdLL, 0xca0709cc398e1cd1LL }, /* a */
    { 0xacd1216d5cc63966LL, 0x534b728e6773c810LL, 0xfb4e95d31a5032feLL, 0xb76e3775f6a4aee7LL }, /* b */
    { 0xfffffffffffffd97LL, 0xffffffffffffffffLL, 0xffffffffffffffffLL, 0xffffffffffffffffLL }, /* p */
    { 0x000000000005cf11LL, 0x0000000000000000LL, 0x0000000000000000LL, 0x0000000000000000LL }, /* r2 */
    { 0xc115af556c360c67LL, 0x0fd8cddfc87b6635LL, 0x0000000000000000LL, 0x4000000000000000LL }, /* q */
    { 0x57cb446240dd1710LL, 0x7556091c4805caa4LL, 0xd0593365f9384bcdLL, 0x0fb1fbc48b0f0eb4LL }, /* r2q */
    {
      { 0x8b2582fe742daa28LL, 0x658b9196932e02c7LL, 0x880923425712b2bbLL, 0x91e38443a5e82c0dLL }, /* px */
      { 0xaf268adb32322e5cLL, 0x5fde0b5344766740LL, 0x895786c4bb46e956LL, 0x32879423ab1a0375LL }, /* py */
      { 0x0000000000000001LL, 0x0000000000000000LL, 0x0000000000000000LL, 0x0000000000000000LL }  /* pz */
    },
    0x46f3234475d5add9LL, /* n */
    0x035bdd1aeafdb0a9LL, /* nq */
    "fffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffd97"
   }; 

Функции
----------------------------------------------

Функция ak_wpoint_set
~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: int ak_wpoint_set( ak_wpoint wp , ak_wcurve wc )

Функция инициализирует и присваивает контексту значение образующей точки эллиптической кривой

   :param wp: Точка P эллиптической кривой, которой присваивается значение, содержащееся в контексте эллиптической кривой.
   :param wc: Эллиптическая кривая, которой принадлежит точка.
   :return: Функция возвращает ak_error_ok. В случае, когда один  из контекстов равен NULL, то возвращается ak_error_null_pointer.

Файл ``example-g06n01.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n01.c
 :language: c
 :linenos:

--------------------------------------------------------------

Функция ak_wpoint_set_as_unit
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: int ak_wpoint_set_as_unit( ak_wpoint wp , ak_wcurve wc )

Функция инициализирует и присваивает контексту значение бесконечно удаленной точки эллиптической кривой

   :param wp: Точка P эллиптической кривой, которой присваивается значение бесконечно удаленной точки.
   :param wc: Эллиптическая кривая, которой принадлежит точка.
   :return: Функция возвращает ak_error_ok. В случае, когда один  из контекстов равен NULL, то возвращается ak_error_null_pointer.

Файл ``example-g06n02.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n02.c
 :language: c
 :linenos:

--------------------------------------------------------------

Функция ak_wpoint_set_wpoint
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: int ak_wpoint_set_wpoint( ak_wpoint wp , ak_wpoint wq , ak_wcurve wc )

Функция инициализирует и присваивает контексту значение заданной точки эллиптической кривой

   :param wp: Точка P эллиптической кривой, которой присваивается новое значение.
   :param wq: Точка Q эллиптической кривой, значение которой присваивается.
   :param wc: Эллиптическая кривая, которой принадлежат обе точки.
   :return:  Функция возвращает ak_error_ok. В случае, когда один  из контекстов равен NULL, то возвращается ak_error_null_pointer.

Файл ``example-g06n03.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n03.c
 :language: c
 :linenos:

--------------------------------------------------------------

Функция ak_wpoint_is_ok
~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: bool_t ak_wpoint_is_ok( ak_wpoint wp , ak_wcurve ec )

Функция проверяет принадлежность точки заданной кривой.

   :param wp: Точка P эллиптической кривой
   :param ec: Эллиптическая кривая, на принадлежность которой проверяется точка P.
   :return: Функция возвращает ak_true если все проверки выполнены. В противном случае возвращается ak_false.

Файл ``example-g06n04.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n04.c
 :language: c
 :linenos:

.. note:: 
   Если точка принадлежит кривой, то функция возращает 1, иначе 0

--------------------------------------------------------------

Функция ak_wpoint_check_order
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: bool_t ak_wpoint_check_order( ak_wpoint wp , ak_wcurve ec )

Функция проверяет порядок заданной точки.

   :param wp: Точка P эллиптической кривой
   :param ec: Эллиптическая кривая, на принадлежность которой проверяется точка P.
   :return: Функция возвращает ak_true если все проверки выполнены. В противном случае возвращается ak_false.

Файл ``example-g06n05.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n05.c
 :language: c
 :linenos:

.. note:: 
   Если порядок точки верный, то функция возращает 1, иначе 0

--------------------------------------------------------------

Функция ak_wpoint_double
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: void ak_wpoint_double( ak_wpoint wp, ak_wcurve ec )

Функция удвоения точки эллиптической кривой, заданной в короткой форме Вейерштрасса

   :param wp: Удваиваемая точка P эллиптической кривой.
   :param ec: Эллиптическая кривая, которой принадлежит точка P.

Файл ``example-g06n06.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n06.c
 :language: c
 :linenos:

--------------------------------------------------------------

Функция ak_wpoint_add
~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: void ak_wpoint_add( ak_wpoint wp1 , ak_wpoint wp2 , ak_wcurve ec )

Функция прибавляет к одной точке эллиптической кривой значения другой точки.

   :param wp1: Точка P, в которую помещается результат операции сложения; первое слагаемое
   :param wp2: Точка Q, второе слагаемое
   :param ec: Эллиптическая кривая, которой принадллежат складываемые точки

Файл ``example-g06n07.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n07.c
 :language: c
 :linenos:

.. important:: 
   Если точки, передаваемые в функцию равны, то выызывается функция
   ak_wpoint_double() 

--------------------------------------------------------------

Функция ak_wpoint_reduce
~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: void ak_wpoint_reduce( ak_wpoint wp , ak_wcurve ec )

Функция приводит проективную точку к аффинному виду.

   :param wp: Точка кривой, которая приводится к аффинной форме
   :param ec: Эллиптическая кривая, которой принадлежит точка

Файл ``example-g06n08.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n08.c
 :language: c
 :linenos:

--------------------------------------------------------------

Функция ak_wpoint_pow
~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: void ak_wpoint_pow( ak_wpoint wq, ak_wpoint wp, ak_uint64 * k, size_t size, ak_wcurve ec )

Функция вычисляет кратную точку эллиптической кривой.

   :param wq: Точка Q, в которую помещается результат.
   :param wp: Точка P, которая возводится в степень.
   :param k: Степень кратности
   :param size: Размер степени k в машинных словах - значение, как правило, задаваемое константой ak_mpzn256_size или ak_mpzn512_size. В общем случае может приниимать любое неотрицательное значение.
   :param ec: Эллиптическая кривая, на которой происходят вычисления

Файл ``example-g06n09.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n09.c
 :language: c
 :linenos:

--------------------------------------------------------------

Функция ak_mpzn_set_wcruve_discriminant
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: void ak_mpzn_set_wcurve_discriminant( ak_uint64 * d, ak_wcurve ec )

Функция вычисляет дискриминант эллиптической кривой, заданной в короткой форме Вейерштрасса.

   :param d: Вычет, в который помещается вычисленное значение.
   :param ec: Контекст эллиптической кривой, для которой вычисляется ее дискриминант

Файл ``example-g06n10.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n10.c
 :language: c
 :linenos:

--------------------------------------------------------------

Функция ak_wcurve_discriminant_is_ok
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: int ak_wcurve_discriminant_is_ok( ak_wcurve ec )

Функция проверяет корректность дискриминанта эллиптической кривой, заданной в форме Вейерштрасса.

   :param ec: Контекст эллиптической кривой, для которой проверяется, что ее дискриминант отличен от нуля.
   :return: Функция возвращает ak_error_ok в случае, если дискриминант отличен от нуля.
    В противном случае возвращается код ошибки. 

Файл ``example-g06n11.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n11.c
 :language: c
 :linenos:

.. note::
   В случае успеха функция возвращает 0, иначе код ошибки.

--------------------------------------------------------------

Функция ak_wcurve_check_order_parameters
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: int ak_wcurve_check_order_parameters( ak_wcurve ec )

Функция проверки корректности параметров, необходимых для вычисления по модулю q.

   :param ec: Контекст структуры эллиптической кривой, содержащий в себе значения параметров. Константные значения структур, которые могут быть использованы библиотекой, задаются в файле ak_parameters.h
   :return: В случае успеха, функция возвращает ak_error_ok. В противном случае, возвращается код ошибки. 

Файл ``example-g06n12.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n12.c
 :language: c
 :linenos:

.. note::
   В случае успеха функция возвращает 0, иначе код ошибки.

--------------------------------------------------------------

Функция ak_wcurve_is_ok
~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. c:function:: int ak_wcurve_is_ok( ak_wcurve ec ) 

Функция проверки набора параметров эллиптической кривой, заданной в форме Вейерштрасса.

   :param ec: Контекст эллиптической кривой.
   :return: Функция возвращает ef ak_error_ok в случае, если параметры определены корректно. В противном случае возвращается код ошибки.

Файл ``example-g06n13.c`` содержит очень простой пример 
использования данной функции

.. literalinclude:: ../examples/faq/example-g06n13.c
 :language: c
 :linenos:

.. note::
   В случае успеха функция возвращает 0, иначе код ошибки. 

Пример вычисления кратной точки со случайным k
-----------------------------------------------

Файл ``example-g06n14.c`` содержит пример вычисления кратной 
точки с использованием случайного числа, сгенерированого через ``struct random``,
а затем принадлежность полученной точки изначальной кривой

.. literalinclude:: ../examples/faq/example-g06n14.c
 :language: c
 :linenos:

Ошибки
---------------

============ =============================== ========
Код ошибки   Название                        Описание
============ =============================== ========      
 -120        ak_error_curve_not_supported    Ошибка, возникающая когда параметры кривой не соответствуют алгоритму, в котором они используются.     
 -121        ak_error_curve_point            Ошибка, возникающая если точка не принадлежит заданной кривой
 -122        ak_error_curve_point_order      Ошибка, возникающая когда порядок точки неверен    
 -123        ak_error_curve_discriminant     Ошибка, возникающая если дискриминант кривой равен нулю (уравнение не задает кривую)
 -124        ak_error_curve_order_parameters Ошибка, возникающая когда неверно определены вспомогательные параметры эллиптической кривой.
 -125        ak_error_curve_prime_modulo     Ошибка, возникающая когда простой модуль кривой задан неверно
 -126        ak_error_curve_not_equal        Ошибка, возникающая при сравнении двух эллиптических кривых 
 -2          ak_error_null_pointer           Ошибка, возникающая при доступе или передаче в качестве аргумента функции null указателя
============ =============================== ======== 

